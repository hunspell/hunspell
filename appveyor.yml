environment:
  matrix:
    - APPVEYOR_BUILD_WORKER_IMAGE: Ubuntu
    - APPVEYOR_BUILD_WORKER_IMAGE: Visual Studio 2019
      MSYSTEM: MINGW64

build_script:
 - cmd: C:\msys64\usr\bin\bash -lc "autoreconf -i && ./configure && make && make check"
 - sh: autoreconf -i && ./configure && make && make check
